# Service account + least-privilege roles for Cloud Run agent (Week 8+).
# Prefer Workload Identity Federation for CI — see docs/WIF.md

output "placeholder" {
  value = "Scaffold for agent runtime SA + IAM bindings."
}
